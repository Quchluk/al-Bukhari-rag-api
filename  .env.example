# Required for loading FAISS vectorstore with OpenAIEmbeddings
OPENAI_API_KEY=sk-...

# Required for calling DeepSeek model via OpenRouter
OPENROUTER_API_KEY=sk-or-...

# Optional: override default FAISS index path (defaults to ./faiss_index)
# FAISS_PATH=./faiss_index

